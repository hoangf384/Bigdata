# cách thêm biến môi trường trong fedora kde

> Fedora kde khá là kì lạ, set biến thẳng vào profile k được (/etc/profile) phải set thành srcipt .sh để auto chạy tự động thêm biến vào môi trường sau khi load profile